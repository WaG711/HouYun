@model IEnumerable<WatchHistory>

@{
    ViewData["Title"] = "История просмотров";
}

<h1 id="Title">История просмотров</h1>

@if (Model.Any())
{
    <p>
        <form method="post" asp-action="DeleteAllWatchHistory" asp-controller="WatchHistory" class="mt-3">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-light btn-sm">Очистить историю</button>
        </form>
    </p>
    <div class="container mt-4">
        <div class="row">
            @foreach (var video in Model)
            {
                <div class="col-md-3">
                    <div class="card video-card">
                        <a href="@Url.Action("Details", "Video", new { videoId = video.Video.VideoId })">
                            <img class="card-img-top poster-image" src="@Url.Content("~/posters/" + video.Video.PosterPath)" alt="Video Poster">
                        </a>
                        <div class="card-body">
                            <h4 class="card-title d-flex justify-content-between">
                                <a href="@Url.Action("Details", "Video", new { videoId = video.VideoId })">@video.Video.Title</a>
                                <div class="dropdown-container">
                                    <div class="dropdown">
                                        <button onclick="toggleDropdown(this)" class="zxc btn btn-outline-secondary btn-sm custom-btn dropdown-toggle">
                                            ...
                                        </button>
                                        <div class="dropdown-menu" style="display: none;">
                                            <a href="@Url.Action("AddToWatchLater", "WatchLater", new { videoId = video.Video.VideoId })" class="dropdown-item">Смотреть позже</a>
                                        </div>
                                    </div>
                                </div>
                            </h4>
                            <p class="card-text">
                                <a href="@Url.RouteUrl("channel", new { channelName = video.Video.Channel.Name })">@video.Video.Channel.Name</a>
                            </p>
                            <p class="card-text">
                                <a href="@Url.Action("Details", "Video", new { videoId = video.Video.VideoId })">@Helper.FormatViews(video.Video.Views.Count) | @Helper.TimeAgo(video.Video.UploadDate)</a>
                            </p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
else
{
    <p>No watch history found.</p>
}